/**
 * The meta-schema validator and tier comparator checked against {@code spec/v1/} (PR 3) — see
 * {@link de.timscho.config.tck.MetaSchemaValidator} and {@link de.timscho.config.tck.TierComparator}.
 *
 * <p>{@code @NullMarked}: every reference type in this package is non-null unless explicitly
 * marked {@link org.jspecify.annotations.Nullable @Nullable}.
 */
@NullMarked
package de.timscho.config.tck;

import org.jspecify.annotations.NullMarked;
